dependencies:
    pre:
        - ssh-agent /bin/bash
        - ssh-add -l
        - ssh-add -k .ssh/id_circleci_github
        - ssh-add -l 
    override:
        - pip install -r requirements.txt
test:
    post:
        - python api/test_mathop

deployment:
    production:
        branch: Production
        commands:
            - fab -f deploy.py deploy:

