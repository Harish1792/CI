machine:
    timezone:
        Asia/Singapore
    python:
        version: 2.7.9

checkout:
    post:
        # append custom ssh_config
        #- cat circleci-data/ssh_config >> ~/.ssh/config

dependencies:
    override:
        # install circleci custom requirements
        #- pip install --upgrade -r circleci-data/pip_requirements.txt
        # install ventesforces2 requirements
        - pip install -r requirements.txt
        #- npm prune && npm install:
        #    pwd: src/assets/
        #- bower prune && bower install:
        #    pwd: src/assets/

test:
    pre:
        -python api/test_mathop

