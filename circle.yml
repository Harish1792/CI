dependencies:
override:
        # install circleci custom requirements
        #- pip install --upgrade -r circleci-data/pip_requirements.txt
        # install ventesforces2 requirements
        - pip install -r requirements.txt
        #- npm prune && npm install:
        #    pwd: src/assets/
        #- bower prune && bower install:
        #    pwd: src/assets/
#dependencies:
#  pre:
#    - eval ssh-agent $SHELL
test:
    post:
        - python api/test_mathop

deployment:
    production:
        branch: Production
        commands:
            - fab -f deploy.py deploy:

